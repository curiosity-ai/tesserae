<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- The optical centering stage measures every glyph in a real browser -->
    <PackageReference Include="Microsoft.Playwright" Version="1.62.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Shells out to fontTools to shift the glyph outlines; kept next to the exe so it is found -->
    <None Update="centre-uicons-outlines.py" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
